<h1> <%= @cocktail.name.capitalize %> </h1>
<ul>
  <% @doses.each  do |dose| %>
    <li> <%= dose.description %>
    <%= dose.ingredient.name %>
      <%= link_to "Delete dose", dose_path(dose), method: "delete", data: { confirm: "Really?" } %>
    </li>
  <% end %>
</ul>

<%= link_to "Add a dose", new_cocktail_dose_path(@cocktail) %>
<%= link_to "Home", cocktails_path %>

